---
title: LLM Wiki 导航
type: nav
tags:
  - navigation
updated: YYYY-MM-DD
---

# LLM Wiki 导航

> **本页是人（你）的入口页。** 列出 `wiki/<topic>/` 下的全部编译页，
> 用**显式全路径链接**，这样 Obsidian 能跳转、Graph View 有边。
>
> ⚠️ 插件读不到本页——它的检索入口只有 `wiki/index.md`（见 `AGENTS.md` 5.1）。
>
> 由 `tools/sync_index.py` 检查是否漏了主题目录；新增/改名知识页时手工补一行。

## 主题目录

*（还没有任何编译页。首次 Ingest 之后，`wiki/<topic>/` 下会出现编译页，
在这里为每一篇补一行显式全路径链接。）*

<!-- 每行的形状（把 <...> 换成实际值）：
- [<页面标题>](wiki/<example-topic>/<页面标题>.md) · 一句话摘要
-->
