# Wiki Index

> 本文件是 obsidian-llm-wiki 插件的**检索入口**（它只读这个文件，`main.js:79003`）。
> 插件会用整文件覆盖的方式重建它，因此**末尾的 AGENT 标记段会被抹掉**。
>
> 标准操作：**插件 ingest / Lint 之后，跑一次 `python tools/sync_index.py --bridge-index` 恢复。**
> 复核：`python tools/sync_index.py --bridge-index --check` 与 `python tools/verify_bridge.py`。
>
> 标记段内由脚本生成，**不要手写**。

<!-- AGENT-INDEX-START -->
<!-- AGENT-INDEX-END -->
