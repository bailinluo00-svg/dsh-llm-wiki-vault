---
title: LLM Wiki 更新公告
type: changelog
tags:
  - changelog
  - system-update
updated: YYYY-MM-DD
---

# LLM Wiki 更新公告

> **本文件只记录「系统更新」** —— 改变这套知识库**怎么运转**的改动。
> 日常的知识摄取（Ingest）、编译知识页、索引重建、日志追加**不算更新**，它们记在 `log.md`。
>
> 读者有两类：**你**（了解系统变成什么样了、需不需要动手）和**将来的 Agent**
> （避免重复已修过的错、不要回退已定的决策）。
> 最新的在最上面。新增条目请用文末的模板。
> **删除了文件的话，除写在对应条目里，还要登记到文末的「已删除文件清单」** —— 那里是全库唯一的删除台账。

> 本模板仓库的更新历史已清空，只保留「怎么写」的规范与模板。
> 理由：原始历史描述的是一个具体知识库的演化，其中包含真实素材信息；
> 格式规范才是系统定义的一部分。你从第一条开始记录即可。

---

## 怎么写新条目

**触发**：任何改动**契约、目录结构、工具脚本、插件配置、依赖、流程规则**之后，
都要在**顶部**加一条。**不触发**：摄入素材、编译知识页、重建索引、追加日志——
这些是内容操作，只进 `log.md`。

**每条必须写清**：

- 一句话标题（带递增编号）+ 日期
- **问题/动机** —— 为什么要改
- **改动** —— 具体改了什么（文件 + 行为）
- **影响** —— 系统变成什么样了；**你要动手吗**（多数是「不需要」，但这句必须写）
- **关键决策（勿回退）** —— 这个决定将来可能被误判时，写清理由

**特别重要**：凡是**推翻了旧结论**或**改变了既定流程**的改动，必须显式写出来。
否则将来的 Agent 会照旧行为办事，把已经修过的坑再踩一遍。

**删除文件时**：除在条目里写明「路径 + 为什么删 + 什么替代了它」，
**还要登记到文末的「已删除文件清单」**——那是全库唯一的删除台账。
清理已删文件的文档引用时，要区分**当前描述**（工具表、目录树、FAQ —— 必须改）
与**历史记录**（`log.md`、本文件的旧条目 —— 不改写历史）。

**编号只增不重排**，最新在最上面，历史条目不改写（与 `log.md` 的追加式原则一致）。

---

## YYYY-MM-DD · #N 一句话标题

**动机**：

**改动**：

**影响**：**你要动手吗**：

**关键决策（勿回退）**：

---

## 已删除文件清单

**全库唯一的删除台账。** 每删一个文件就加一行。按时间倒序，只增不删。

| 删除日期 | 路径 | 为什么删 | 替代方案 |
|---|---|---|---|
| | | | |
